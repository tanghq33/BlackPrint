name: Build and Release

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-26
    timeout-minutes: 30

    steps:
      - name: Set up SSH key for submodule
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_KEY }}

      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set version
        id: version
        run: |
          TAG="${GITHUB_REF_NAME}"
          VERSION="${TAG#v}"
          echo "tag=$TAG"         >> "$GITHUB_OUTPUT"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Build (Release)
        run: |
          rm -rf build && mkdir -p build
          /Applications/Xcode.app/Contents/Developer/usr/bin/xcodebuild \
            -project BlackPrint.xcodeproj \
            -scheme BlackPrint \
            -configuration Release \
            -derivedDataPath build/DerivedData \
            -destination "platform=macOS" \
            CODE_SIGN_STYLE=Manual \
            CODE_SIGN_IDENTITY="-" \
            CODE_SIGNING_ALLOWED=NO \
            DEVELOPMENT_TEAM="" \
            MARKETING_VERSION=${{ steps.version.outputs.version }} \
            CURRENT_PROJECT_VERSION=${{ steps.version.outputs.version }} \
            build

      - name: Ad-hoc sign
        run: |
          APP="build/DerivedData/Build/Products/Release/BlackPrint.app"
          xattr -cr "$APP"
          codesign --force --deep --sign - \
            --entitlements BlackPrint/BlackPrint.entitlements "$APP"

      - name: Create DMG
        id: dmg
        run: |
          APP="build/DerivedData/Build/Products/Release/BlackPrint.app"
          DMG="build/BlackPrint-${{ steps.version.outputs.version }}.dmg"
          bash scripts/create-dmg/create-dmg \
            --volname "BlackPrint" \
            --window-size 500 320 \
            --icon-size 96 \
            --icon "BlackPrint.app" 150 160 \
            --app-drop-link 350 160 \
            --no-internet-enable \
            --skip-jenkins \
            "$DMG" "$APP"
          echo "dmg=$DMG" >> "$GITHUB_OUTPUT"

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: BlackPrint ${{ steps.version.outputs.version }}
          files: ${{ steps.dmg.outputs.dmg }}
          fail_on_unmatched_files: true
